version: '3'
services:
    training-endpoint:
        build: . 
        ports:
            - 9999:9999
        expose:
            - 9999
        networks:
            - mlops_network
        deploy:
            resources:
                reservations:
                    devices:
                        - capabilities: [gpu]
networks:
    mlops_network:
        external: true
